#include "main.h"

/**
 * puts2 -  Resetting a value
 * @str: parameter
 * Return: a value
 */

void puts2(char *str)
{
	int i;

	for (; i <= str[i]; i++)
	{
		if (i % 2 == 0)
			_putchar(str[i]);
	}
}
