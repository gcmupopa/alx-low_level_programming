#include <stdio.h>
#include <stdlib.h>

/**
 * main - print the number of arguments passed
 * @argc: parameter
 * @argv: parameter
 * Return: a value
 */

int main(int argc, char *argv[])
{
	int i;
	void argv;

	for (i = 0; i <= argc; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
