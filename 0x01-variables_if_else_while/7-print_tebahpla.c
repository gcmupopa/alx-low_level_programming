#include <stdio.h>

/**
 * main - This prints the lowercase alphabet in reverse
 * Return: This returns 0
 */

int main(void)
{
	char x = 'z';

	while
		(x >= 'a')
		{
			putchar(x);
			x--;
		}
	putchar('\n');
	return (0);
}
