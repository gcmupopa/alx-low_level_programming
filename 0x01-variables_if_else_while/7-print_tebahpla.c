#include <stdio.h>

/**
 * main - This prints the lowercase alphabet in reverse
 * Return: This returns 0
 */

int main(void)
{
	int x = 122;

	while
		(x >= 97)
		{
			putchar(x);
			x++;
		}
	putchar('\n');
	return (0);
}
