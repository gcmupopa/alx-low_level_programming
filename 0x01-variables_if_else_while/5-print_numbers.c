#include <stdio.h>

/**
 * main - This prints all single digit numbers of base 10 starting from 0
 * Return: This returns 0
 */

int main(void)
{
	int x = 48;

	while
		(x <= 57)
		{
			putchar(x);
			x++;
		}
	putchar('\n');
	return (0);
}
