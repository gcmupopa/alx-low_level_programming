#include "main.h"

/**
 * _abs - print value
 * @n: input parameter
 * Description: computes absolute value of an integer
 * Return: This returns string value
 */

int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
	return (0);
}
