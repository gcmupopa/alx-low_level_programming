#include "main.h"

/**
 * print_sign - prints sign of a number
 * @n: parameter to be checked
 * Description: prints the sign of a number
 * Return: This returns 1 or 0 or -1
 */

int print_sign(int n)
{
	int n;

	if (n >)
		return (1);
		_putchar('+');
	else if (n == 0)
		return (0);
		_putchar('0');
	else if (n < 0)
		return (-1);
		_putchar('-');
	}
